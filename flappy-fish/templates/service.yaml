apiVersion: v1
kind: Service
metadata:
  name: {{ include "flappy-fish.fullname" . }}
  labels:
    {{- include "flappy-fish.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flappy-fish.selectorLabels" . | nindent 4 }}
