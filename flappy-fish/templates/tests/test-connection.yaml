apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flappy-fish.fullname" . }}-test-connection"
  labels:
    {{- include "flappy-fish.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flappy-fish.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
